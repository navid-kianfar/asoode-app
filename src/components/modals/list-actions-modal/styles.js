import { StyleSheet } from "react-native";

export default StyleSheet.create({
  color: {
    width: 40,
    height: 30,
    borderRadius: 5,
    justifyContent: "center",
    alignItems: "center",
    margin: 5
  }
});
