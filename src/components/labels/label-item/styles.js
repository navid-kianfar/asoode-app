import { StyleSheet } from "react-native";

export default StyleSheet.create({
  label: {
    flex: 1,
    borderRadius: 10,
    alignItems: "center",
    height: 45,
    paddingHorizontal: 10
  }
});
