import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  backButton: {
    position: "absolute",
    top: 10,
    left: 0
  },
  name: {},
  userName: {},
  bio: {}
});

export default styles;
