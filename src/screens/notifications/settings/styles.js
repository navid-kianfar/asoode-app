import { StyleSheet } from "react-native";
import { Metrics } from "../../../themes/variables";

const styles = StyleSheet.create({
  headerImage: {
    width: Metrics.WIDTH * 0.7,
    height: Metrics.WIDTH * 0.7 * 0.507
  }
});

export default styles;
